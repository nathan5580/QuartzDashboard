bin/
obj/
*.user
*.suo
.vs/
*.log
.DS_Store
nupkg/

# Local demo runtime artifacts (SQLite history store from `dotnet run --sqlite`)
demo-history.db
demo-history.db-shm
demo-history.db-wal

# UX-audit artefacts (kept locally for review; not part of the package)
ux-audit-screenshots/persona-*/
ux-audit-screenshots/batch*/

# JetBrains Rider
.idea/
*.sln.iml

# Hermes agent working files (plans, logs, state)
.hermes/

# Node modules (for esbuild tooling)
node_modules/

# Playwright MCP cache/artifacts
.playwright-mcp/

# Minified build output (generated by esbuild, committed for CI)
# *.min.js and *.min.css are tracked — they're generated from source
!**/alpine.min.js
!**/signalr.min.js
